<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link href="/favicon.png" rel="shortcut icon" type="image/x-icon" />
    <meta name="generator" content={Astro.generator} />
    <title>Triple Whale | Moby Slack App Support</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    /* Light theme variables */
    --bg-gradient-light: linear-gradient(135deg, #f0f4f8 0%, #e2eaf2 100%);
    --text-color-light: #1a1a1a;
    --heading-gradient-light: linear-gradient(90deg, #032045, #053366);
    --link-color-light: #0066cc;
    --box-bg-light: rgba(0, 0, 0, 0.05);

    /* Dark theme variables (existing) */
    --bg-gradient-dark: linear-gradient(135deg, #032045 0%, #053366 100%);
    --text-color-dark: #f0f4f8;
    --heading-gradient-dark: linear-gradient(90deg, #e2f1ff, #b3d5ff);
    --link-color-dark: #4a9eff;
    --box-bg-dark: rgba(255, 255, 255, 0.05);
  }

  body {
    font-family: 'Inter', sans-serif;
    background: var(--bg-gradient-light);
    color: var(--text-color-light);
    line-height: 1.6;
    padding: 0;
    margin: 0;
  }

  .dark body {
    background: var(--bg-gradient-dark);
    color: var(--text-color-dark);
  }

  .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 3rem 2rem;
  }

  @media (max-width: 768px) {
    .container {
      padding: 2rem 1.25rem;
    }
  }

  h1 {
    font-size: 2.5rem;
    margin-bottom: 0;
    color: var(--text-color-light);
    background: var(--heading-gradient-light);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .dark h1 {
    color: var(--text-color-dark);
    background: var(--heading-gradient-dark);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  h2 {
    font-size: 1.8rem;
    margin-top: 3rem;
    margin-bottom: 0;
    color: #032045;
  }

  .dark h2 {
    color: #e2f1ff;
  }

  h3 {
    font-size: 1.4rem;
    margin-top: 2rem;
    margin-bottom: 0;
    color: #053366;
  }

  .dark h3 {
    color: #c6ddfa;
  }

  p {
    font-size: 1.1rem;
    margin-bottom: 1rem;
  }

  a:not(.header-link) {
    color: var(--link-color-light);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .dark a:not(.header-link) {
    color: var(--link-color-dark);
  }

  a:not(.header-link):hover {
    color: #0052a3;
    text-decoration: underline;
  }

  .dark a:not(.header-link):hover {
    color: #66b2ff;
  }

  footer,
  footer p {
    display: flex;
    align-items: center;
  }

  footer {
    justify-content: space-between;
    margin-top: 4rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(0, 0, 0, 0.1);
  }

  .dark footer {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  footer p {
    justify-content: flex-start;
    gap: 1rem;
    margin: 0;
    font-size: 0.9rem;
  }

  .box {
    background: var(--box-bg-light);
    border-radius: 8px;
    padding: 1.5rem;
    margin: 1.5rem 0;
    border: 1px solid rgba(0, 0, 0, 0.1);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
  }

  .dark .box {
    background: var(--box-bg-dark);
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
  }

  .videoWrapper {
    position: relative;
    padding-bottom: 56.25%; /* 16:9 */
    height: 0;
    border-radius: 12px;
    overflow: hidden;
    margin: 1.5rem 0;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
  }

  .dark .videoWrapper {
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
  }

  .videoWrapper iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  .header-link {
    color: inherit;
    text-decoration: none;
  }

  .header-link:hover::after {
    content: ' #';
    opacity: 0.5;
  }
</style>
